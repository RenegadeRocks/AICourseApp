# Only needed if you swap MockProvider for AnthropicProvider (see src/provider.ts).
# The eval harness and tests run fully offline with MockProvider — no key required.
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-sonnet-5
